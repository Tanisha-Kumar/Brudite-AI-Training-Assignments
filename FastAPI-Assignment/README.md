## Running the Application on Port 8080

By default, Uvicorn runs on port 8000.
To change the port to 8080, run the following command:
```bash
uvicorn main:app --reload --port 8080
```
## URL:
- Swagger http://localhost:8080/docs
- ReDoc http://localhost:8080/redocs